@model Database.Models.Genero
@{
    ViewData["Title"] = "Eliminar Genero";
}

<div class="row">
    <div class="offset-12">
        <div class="w-100">
            <p class="alert alert-danger">Seguro que quieres eliminar este genero ?</p>
        </div>
        <div class="card border">
            <div class="card-header bg-dark">
                <h3 class="text-white"><strong>Eliminar Genero</strong></h3>
            </div>
            <div class="card-body">
                <form asp-action="BorrarGenero" method="post">

                    <!--ID del genero como campo oculto-->
                    <input type="hidden" asp-for="Id" />

                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group mb-3">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" readonly />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <button type="submit" class="btn btn-danger d-block mx-auto"><i class="bi bi-eraser-fill"></i>Eliminar</button>
                    </div>

                </form>

                <a asp-controller="Genero" asp-action="GListado">Volver</a>
            </div>
        </div>
    </div>
</div>

